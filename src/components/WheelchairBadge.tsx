const WheelchairBadge = () => {
  return (
    <div className="inline-flex items-center gap-2 bg-accent/20 text-accent-foreground px-3 py-1.5 rounded-full text-sm font-medium border border-accent/30">
      <span>🦽</span>
      <span>휠체어 경로 모드</span>
    </div>
  );
};

export default WheelchairBadge;
